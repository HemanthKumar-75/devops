- name: filters
  hosts: local # 192.168.21.32
  connection: local
  # become: yes
  name: HemantH KuMaR
  tasks:
  - name: convert the details to upper
    ansible.builtin.debug:
      msg: "name in upper: {{name | upper}}"

