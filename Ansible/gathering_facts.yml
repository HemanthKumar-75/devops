- name: Gathering Facts
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: Gathering all the onfiration generated by Ansible
    ansible.builtin.debug:
      msg: "{{ ansible_facts }}"